'use server'

export const registerTenantSubmit = async (e) => {
    e.preventDefault();
   /* const { serverRuntimeConfig } = getConfig() || {};
    const tenant_name = document.getElementById('tenant_name');
    const domain_url = document.getElementById('domain_url');
    const tenant_title = document.getElementById('tenant_title');
    const tenant_logo = document.getElementById('tenant_logo');
    const tenant_description = document.getElementById('tenant_description');
    const contact_person = document.getElementById('contact_person');
    const contact_email = document.getElementById('contact_email');
    const contact_phone = document.getElementById('contact_phone');
    const contact_address = document.getElementById('contact_address');
    const subscription_start_date = document.getElementById('subscription_start_date');
    const subscription_end_date = document.getElementById('subscription_end_date');
    const subscription_type = document.getElementById('subscription_type');
    const location_country = document.getElementById('location_country');
    const location_state = document.getElementById('location_state');
    const location_zip = document.getElementById('location_zip');
    const tenant_status = document.getElementById('tenant_status');
  
    
    //let tenantname = '';
    //let domailurl = '';
    let data = {}
    try {
      console.log(tenant_name.value)
      const tenantname = tenant_name.value;   
      const domainurl = domain_url.value;    
      const tenantitle = tenant_title.value;    
      const tenantlogo = tenant_logo.value;    
      const tenantdescription = tenant_description.value; 
      const contactperson = contact_person.value;
      const contactemail = contact_email.value;
      const contactphone = contact_phone.value;
      const contactaddress = contact_address.value;
      const subscriptionstartdate = subscription_start_date.value;
      const subscriptionenddate = subscription_end_date.value;
      const subscriptiontype = subscription_type.value;
      const locationcountry = location_country.value;
      const locationstate = location_state.value;
      const locationzip = location_zip.value;
      const tenantstatus = tenant_status.value;    
      data = {
        "tenantname": tenantname,
        "domainurl": domainurl,
        "tenantitle": tenantitle,
        "tenantlogo": tenantlogo,
        "tenantdescription": tenantdescription,
        "contactperson": contactperson,
        "contactemail": contactemail,
        "contactphone": contactphone,
        "contactaddress": contactaddress,
        "subscriptionstartdate": subscriptionstartdate,
        "subscriptionenddate": subscriptionenddate,
        "subscriptiontype": subscriptiontype,
        "locationcountry": locationcountry,
        "locationstate": locationstate,
        "locationzip": locationzip,
        "tenantstatus": tenantstatus
      }   
      
    } catch (err) { }
  */
    /*const formData = new FormData();
    data.entries(obj).forEach(([key, value]) => {
      formData.append(key, value);
    });*/
    
       
    /*
    let accessToken = ''
    let apiBackendURL = ''
    let tenantID = 0
    // get server side global store data
    if (serverRuntimeConfig) {
      // get api backend url
      apiBackendURL = serverRuntimeConfig.API_BACKEND_SERVER
      // get access token
      accessToken = serverRuntimeConfig.API_ACCESS_TOKEN_SERVER
      tenantID = serverRuntimeConfig.TENANT_ID
    }
  
    console.log('66666666666666',accessToken)
    
    console.log('token here', accessToken)
    if (tenantname && domailurl) {
  
        let res = await registerTenantAction(accessToken, apiBackendURL, formData);
  
    } else {
        console.log("Invalid tenant name and URL.")
  
    }*/
  };
  
  
  
  